export type ModalItem = {
  id: number;
  img: string;
  title: string;
  desc: string;
};

export type ProjectItem = {
  id: number;
  img: string;
  title: string;
  stack: string;
  desc: string;
  url: string;
};

export type StudyItem = {
  index: number;
  topic: string;
  desc: string;
  github_link: string;
  blog_link: string;
};
